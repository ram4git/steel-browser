{
  "name": "Steel Browser",
  "user": "ubuntu",
  "install": "npm install && npm run prepare:recorder -w api",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev -w api",
      "description": "Steel Browser API (Fastify + Chrome via CDP) on http://localhost:3000. Swagger/Scalar docs at /documentation."
    },
    {
      "name": "ui",
      "command": "npm run dev -w ui",
      "description": "Steel Browser UI (Vite dev server) on http://localhost:5173, proxied to the API."
    }
  ],
  "ports": [
    {
      "name": "api",
      "port": 3000
    },
    {
      "name": "ui",
      "port": 5173
    },
    {
      "name": "chrome-cdp",
      "port": 9222
    }
  ]
}
